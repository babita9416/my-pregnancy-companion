import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.abhishek.pregnancycompanion',
  appName: 'My Pregnancy Companion',
  webDir: 'dist'
};

export default config;
